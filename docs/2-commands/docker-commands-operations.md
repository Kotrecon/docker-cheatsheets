# Docker Commands & Operations - Обзор

Полное руководство по работе с Docker через командную строку. Все основные команды, их параметры и практические сценарии использования для .NET 7 и JavaScript/TypeScript разработки.

## 📚 Содержание раздела

### 🔧 Структурированные справочники команд

- [**Docker CLI Reference**](0-docker-cli-reference.md) - Полный структурированный справочник по всем основным командам Docker
- [**Dockerfile Reference**](4-dockerfile-reference.md) - Исчерпывающий гайд по инструкциям Dockerfile для .NET 7 и Node.js
- [**Advanced Docker Commands**](3-advanced-docker-commands.md) - Редко используемые и специализированные команды

### 🚀 Оркестрация и управление

- [**Docker Compose Guide**](1-docker-compose-guide.md) - Работа с multi-container приложениями (.NET 7 + JS/TS примеры)
- [**Docker System Management**](2-docker-system-management.md) - Управление системными ресурсами Docker

## 🏗️ Структура команд Docker

```bash
Основные категории команд:
│
├── 🐳 Контейнеры (docker run, ps, exec, rm, stop, start)
├── 📦 Образы (docker build, images, pull, push, rmi, history)
├── 🌐 Сети (docker network, port, inspect)
├── 💾 Тома (docker volume, cp)
├── 🔧 Система (docker info, system, stats, events)
├── 🚀 Compose (docker compose up, down, logs, exec)
├── 🛠️  Проекты (docker init, scan, trust)
└── 📝 Dockerfile (FROM, RUN, COPY, CMD, EXPOSE)
```

## 🔄 Процесс работы с Docker CLI

### Базовый рабочий процесс

1. **Инициализация**: `docker init` - создание проекта с готовыми шаблонами
2. **Сборка**: `docker build` - создание образов из Dockerfile
3. **Запуск**: `docker run` / `docker compose up` - развертывание контейнеров
4. **Управление**: `docker ps`, `docker exec` - мониторинг и взаимодействие
5. **Мониторинг**: `docker logs`, `docker stats` - наблюдение за работой
6. **Остановка**: `docker stop`, `docker rm` - очистка ресурсов
7. **Обслуживание**: `docker system prune` - очистка системы

### Продвинутый workflow

```bash
# Разработка .NET приложения
docker init                            # Инициализация .NET проекта
docker build -t myapp .                # Сборка образа
docker compose up -d                   # Запуск с зависимостями
docker compose logs -f api             # Мониторинг логов
docker compose exec api bash           # Отладка в контейнере

# Разработка Node.js приложения
docker build -f Dockerfile.dev -t myapp .
docker run -p 3000:3000 -v $(pwd):/app myapp
```

## 📖 Как изучать команды

Рекомендуемый порядок изучения для .NET и JS/TS разработчиков:

### 🟢 Уровень 1: Основы (день 1-3)

1. **Работа с контейнерами** (`run`, `ps`, `exec`, `rm`, `stop`, `start`)
2. **Базовые образы** (`build`, `images`, `pull`, `rmi`)
3. **Логи и мониторинг** (`logs`, `stats`)

### 🟡 Уровень 2: Комплексные приложения (день 4-7)

1. **Docker Compose** (`up`, `down`, `logs`, `exec`) - для multi-container приложений
2. **Dockerfile основы** (FROM, RUN, COPY, CMD, EXPOSE) - создание кастомных образов

### 🔵 Уровень 3: Продвинутые сценарии (день 8-14)

1. **Сети и тома** (`network`, `volume`, `cp`) - для сложных конфигураций
2. **Системные команды** (`system`, `info`, `events`) - мониторинг и управление
3. **Безопасность** (`scan`, `trust`) - безопасная работа с образами

### 🟣 Уровень 4: Экспертные знания (день 15+)

1. **Продвинутые команды** (`update`, `checkpoint`, `buildx`) - специфические сценарии
2. **Оптимизация** - лучшие практики для продакшена

## 🎯 Ключевые принципы работы

- **Consistency** (Последовательность) - одинаковое поведение на всех окружениях
- **Reproducibility** (Воспроизводимость) - идентичные результаты при повторных запусках
- **Efficiency** (Эффективность) - использование кеширования и оптимизаций
- **Security** (Безопасность) - минимальные привилегии и изоляция
- **Dev-Prod Parity** (Паритет сред) - идентичные окружения разработки и продакшена

## 🛠️ Специфика для .NET 7 и JavaScript/TypeScript

### Для .NET 7 разработчиков

- Используйте многостадийные сборки для уменьшения размеров образов
- Настраивайте health checks для проверки готовности приложений
- Используйте официальные образы от Microsoft (`mcr.microsoft.com/dotnet/`)

### Для JavaScript/TypeScript разработчиков

- Настраивайте hot reload для эффективной разработки
- Используйте alpine образы для минимального размера
- Оптимизируйте установку зависимостей с помощью layer caching

## ✅ Практические проверки знаний

### Базовые проверки

```bash
# Работа с контейнерами
docker ps -a
docker run --rm alpine echo "Hello Docker"
docker exec <container> whoami

# Работа с образами
docker images
docker build -t test-image .
docker history test-image
```

### Проверки для .NET

```bash
# Сборка и запуск .NET приложения
docker build -t dotnet-app .
docker run -p 8080:80 dotnet-app

# Работа с Docker Compose для .NET
docker compose up -d
docker compose logs api
```

### Проверки для Node.js

```bash
# Сборка и запуск Node.js приложения
docker build -t node-app .
docker run -p 3000:3000 node-app

# Разработка с hot reload
docker build -f Dockerfile.dev -t node-dev .
docker run -p 3000:3000 -v $(pwd):/app node-dev
```

### Системные проверки

```bash
# Мониторинг системы
docker info
docker system df
docker stats --no-stream

# Управление проектами
docker init --help
docker compose version
docker scan <image>
```

---
