Отлично! Поехали! Создаю структурированный справочник команд Docker.

---

**[file name]: 0 - Docker CLI Reference.md**
**[file content begin]**

# Docker CLI Reference

Полный структурированный справочник по командам Docker CLI с примерами использования и ключевыми параметрами.

## 🐳 Работа с контейнерами

### `docker run`

**Запуск нового контейнера**

```bash
docker run [OPTIONS] IMAGE [COMMAND] [ARG...]
```

**Основные опции:**

- `-d` - запуск в фоновом режиме
- `--name` - имя контейнера
- `-p` - проброс портов (host:container)
- `-v` - подключение томов
- `--rm` - автоматическое удаление после остановки
- `-e` - установка переменных окружения

**Примеры:**

```bash
docker run -d --name web nginx:latest
docker run -it --rm alpine /bin/sh
docker run -p 8080:80 -v /data:/app nginx
```

### `docker ps`

**Просмотр контейнеров**

```bash
docker ps [OPTIONS]
```

**Опции:**

- `-a` - все контейнеры (включая остановленные)
- `-q` - только ID контейнеров
- `--filter` - фильтрация по условиям
- `--format` - форматирование вывода

**Примеры:**

```bash
docker ps -a
docker ps --filter "status=running"
docker ps --format "table {{.Names}}\t{{.Status}}"
```

### `docker exec`

**Выполнение команд в работающем контейнере**

```bash
docker exec [OPTIONS] CONTAINER COMMAND [ARG...]
```

**Опции:**

- `-it` - интерактивный режим с TTY
- `--user` - выполнение от имени пользователя
- `-e` - установка переменных окружения

**Примеры:**

```bash
docker exec -it web /bin/bash
docker exec --user app_user my_container whoami
docker exec web ls -la /app
```

### `docker diff`

**Просмотр изменений в контейнере**

```bash
docker diff CONTAINER
```

**Типы изменений:**

- `A` - файл добавлен
- `D` - файл удален
- `C` - файл изменен

**Пример:**

```bash
docker diff my_container
# A /tmp/new_file
# D /var/log/old.log
```

### `docker stop/start/restart`

**Управление состоянием контейнеров**

```bash
docker stop CONTAINER [CONTAINER...]
docker start CONTAINER [CONTAINER...]
docker restart CONTAINER [CONTAINER...]
```

### `docker rm`

**Удаление контейнеров**

```bash
docker rm [OPTIONS] CONTAINER [CONTAINER...]
```

**Опции:**

- `-f` - принудительное удаление работающих контейнеров
- `-v` - удаление связанных томов

---

## 📦 Работа с образами

### `docker build`

**Сборка образа из Dockerfile**

```bash
docker build [OPTIONS] PATH | URL | -
```

**Основные опции:**

- `-t` - тег для образа
- `--no-cache` - сборка без использования кеша
- `-f` - указание Dockerfile
- `--build-arg` - передача аргументов сборки

**Примеры:**

```bash
docker build -t myapp:latest .
docker build --no-cache -t myapp:clean .
docker build -f Dockerfile.prod -t myapp:prod .
```

### `docker images`

**Просмотр образов**

```bash
docker images [OPTIONS] [REPOSITORY[:TAG]]
```

**Опции:**

- `-a` - все образы (включая промежуточные)
- `-q` - только ID образов
- `--digests` - показать дайджесты

### `docker pull/push`

**Загрузка и выгрузка образов**

```bash
docker pull [OPTIONS] NAME[:TAG|@DIGEST]
docker push [OPTIONS] NAME[:TAG]
```

### `docker rmi`

**Удаление образов**

```bash
docker rmi [OPTIONS] IMAGE [IMAGE...]
```

**Опции:**

- `-f` - принудительное удаление

### `docker history`

**История сборки образа**

```bash
docker history [OPTIONS] IMAGE
```

**Пример:**

```bash
docker history nginx:latest
```

---

## 🔧 Системные команды

### `docker info`

**Информация о системе Docker**

```bash
docker info [OPTIONS]
```

Показывает:

- Версию Docker
- Количество контейнеров и образов
- Драйвер хранения
- Сетевые настройки
- Системные ресурсы

### `docker version`

**Версия Docker**

```bash
docker version [OPTIONS]
```

### `docker stats`

**Статистика в реальном времени**

```bash
docker stats [OPTIONS] [CONTAINER...]
```

**Опции:**

- `--no-stream` - однократный вывод
- `--format` - форматирование вывода

**Примеры:**

```bash
docker stats
docker stats --format "table {{.Container}}\t{{.CPUPerc}}\t{{.MemUsage}}"
docker stats --no-stream
```

### `docker system`

**Управление системой**

```bash
docker system df          # Использование диска
docker system prune       # Очистка неиспользуемых данных
docker system events      # Системные события
```

---

## 🛠️ Команды проектов

### `docker init`

**Инициализация Docker-проекта**

```bash
docker init
```

Создает файлы:

- `.dockerignore`
- `Dockerfile`
- `compose.yaml`
- `README.Docker.md`

Поддерживает платформы:

- Node.js, Python, Go, Java, Rust
- PHP, ASP.NET Core
- Общие шаблоны

---

## 📁 Работа с файлами

### `docker cp`

**Копирование файлов между хостом и контейнером**

```bash
docker cp [OPTIONS] CONTAINER:SRC_PATH DEST_PATH
docker cp [OPTIONS] SRC_PATH CONTAINER:DEST_PATH
```

**Примеры:**

```bash
docker cp my_container:/app/logs ./logs/
docker cp config.json my_container:/app/config/
```

---

## 🎯 Быстрый старт

### Базовый рабочий процесс

```bash
# Сборка образа
docker build -t myapp .

# Запуск контейнера
docker run -d -p 80:80 --name web myapp

# Проверка статуса
docker ps

# Просмотр логов
docker logs web

# Остановка и удаление
docker stop web
docker rm web
```

### Полезные комбинации

```bash
# Очистка системы
docker system prune -a -f

# Просмотр всех ID контейнеров
docker ps -aq

# Удаление всех остановленных контейнеров
docker rm $(docker ps -aq)

# Просмотр использования ресурсов
docker stats --format "table {{.Name}}\t{{.CPUPerc}}\t{{.MemPerc}}"
```

---

## 🔍 Примечания

- **Форматирование вывода**: Используйте `--format` для удобного отображения данных
- **Фильтрация**: `--filter` помогает находить нужные контейнеры и образы
- **Автодополнение**: Настройте автодополнение для вашей оболочки
- **Логи**: Используйте `docker logs` для просмотра вывода контейнеров

Этот справочник охватывает 90% повседневных задач работы с Docker.
[file content end]

---

Теперь создаем следующий файл? Предлагаю **`1 - Docker Compose Guide.md`** для работы с multi-container приложениями. Продолжаем? 🚀
