# Docker System Management

Полное руководство по мониторингу, обслуживанию и управлению системными ресурсами Docker.

## 📊 Мониторинг системы

### `docker stats`

**Мониторинг ресурсов в реальном времени**

```bash
docker stats [OPTIONS] [CONTAINER...]
```

**Опции:**

- `--all` / `-a` - показать все контейнеры (по умолчанию только работающие)
- `--no-stream` - однократный вывод статистики
- `--format` - форматирование вывода
- `--no-trunc` - не обрезать вывод

**Примеры:**

```bash
# Базовая статистика
docker stats

# Статистика конкретных контейнеров
docker stats web db redis

# Однократный вывод
docker stats --no-stream

# Форматированный вывод
docker stats --format "table {{.Name}}\t{{.CPUPerc}}\t{{.MemUsage}}\t{{.NetIO}}"

# Все контейнеры с подробным выводом
docker stats -a --no-trunc
```

**Форматы вывода:**

```bash
# Простой формат
docker stats --format "{{.Name}}: {{.CPUPerc}} CPU, {{.MemUsage}}"

# Табличный формат
docker stats --format "table {{.Container}}\t{{.Name}}\t{{.CPUPerc}}\t{{.MemPerc}}"

# JSON формат
docker stats --format "{{json .}}"
```

### `docker system df`

**Анализ использования дискового пространства**

```bash
docker system df [OPTIONS]
```

**Опции:**

- `-v` / `--verbose` - подробная информация
- `--format` - форматирование вывода

**Примеры:**

```bash
# Базовая информация
docker system df

# Подробная информация
docker system df -v

# Форматированный вывод
docker system df --format "table {{.Type}}\t{{.TotalCount}}\t{{.Size}}"
```

**Вывод включает:**

- **Images** - используемые образы
- **Containers** - контейнеры и их размеры
- **Local Volumes** - локальные тома
- **Build Cache** - кеш сборки

## 🧹 Очистка системы

### `docker system prune`

**Очистка неиспользуемых данных**

```bash
docker system prune [OPTIONS]
```

**Опции:**

- `-a` / `--all` - удалить все неиспользуемые образы
- `--volumes` - удалить неиспользуемые тома
- `-f` / `--force` - не запрашивать подтверждение
- `--filter` - фильтрация по условиям

**Примеры:**

```bash
# Базовая очистка (только контейнеры и сети)
docker system prune

# Полная очистка (включая образы)
docker system prune -a

# Очистка с томами
docker system prune --volumes

# Принудительная очистка
docker system prune -a -f

# Очистка с фильтром
docker system prune --filter "until=24h"
```

### Целевая очистка

**Контейнеры:**

```bash
# Удалить все остановленные контейнеры
docker container prune

# Удалить контейнеры старше 24 часов
docker container prune --filter "until=24h"
```

**Образы:**

```bash
# Удалить все неиспользуемые образы
docker image prune -a

# Удалить образы старше недели
docker image prune -a --filter "until=168h"
```

**Тома:**

```bash
# Удалить неиспользуемые тома
docker volume prune

# Удалить тома с фильтром
docker volume prune --filter "label!=important"
```

**Сети:**

```bash
# Удалить неиспользуемые сети
docker network prune
```

**Кеш сборки:**

```bash
# Очистка кеша сборки
docker builder prune
```

## 🔍 Детальная информация

### `docker system info`

**Подробная информация о системе Docker**

```bash
docker info [OPTIONS]
```

**Показывает:**

- Версии Docker, API, Go
- Количество контейнеров и образов
- Драйвер хранения и файловой системы
- Сетевые настройки и драйверы
- Информацию о хосте
- Ресурсы системы

### `docker system events`

**Мониторинг системных событий**

```bash
docker system events [OPTIONS]
```

**Опции:**

- `--filter` - фильтрация событий
- `--since` - события с указанного времени
- `--until` - события до указанного времени
- `--format` - форматирование вывода

**Примеры:**

```bash
# Все события в реальном времени
docker system events

# События за последний час
docker system events --since 1h

# Только события создания контейнеров
docker system events --filter type=container --filter event=create

# События с форматированием
docker system events --format "{{.Type}} {{.Action}} {{.Actor.ID}}"
```

## 📈 Анализ производительности

### Мониторинг ресурсов

```bash
# Постоянный мониторинг
docker stats

# Однократный снимок
docker stats --no-stream

# Мониторинг с сохранением в файл
docker stats --no-stream > docker_stats.log
```

### Анализ использования диска

```bash
# Общий обзор
docker system df

# Детальный анализ образов
docker images --format "table {{.Repository}}\t{{.Tag}}\t{{.Size}}"

# Размеры контейнеров
docker ps -s --format "table {{.Names}}\t{{.Size}}"
```

## 🛠️ Управление ресурсами

### Ограничения ресурсов

```bash
# Запуск с ограничениями CPU и памяти
docker run -it --cpus="1.5" --memory="512m" alpine

# Ограничение ввода-вывода
docker run --device-write-bps /dev/sda:1mb alpine
```

### Проверка конфигурации

```bash
# Проверка лимитов контейнера
docker inspect --format='{{.HostConfig.CpuShares}}' container_name
docker inspect --format='{{.HostConfig.Memory}}' container_name
```

## 🗑️ Сценарии очистки

### Ежедневная очистка

```bash
# Безопасная очистка (сохраняет тома и недавние образы)
docker system prune -f
```

### Еженедельная очистка

```bash
# Полная очистка (включая неиспользуемые образы)
docker system prune -a -f
```

### Очистка перед деплоем

```bash
# Максимальная очистка
docker system prune -a --volumes -f
```

### Целевая очистка по тегам

```bash
# Удалить все образы с тегом <none>
docker images --filter "dangling=true" -q | xargs docker rmi

# Удалить образы по паттерну
docker images | grep "test-" | awk '{print $3}' | xargs docker rmi
```

## 📋 Чеклист обслуживания

### Ежедневно

```bash
# Проверка состояния
docker ps -a
docker system df

# Мониторинг ресурсов
docker stats --no-stream
```

### Еженедельно

```bash
# Очистка системы
docker system prune -f

# Проверка событий
docker system events --since 168h --until now
```

### Ежемесячно

```bash
# Полная очистка
docker system prune -a -f

# Анализ больших образов
docker images --format "table {{.Size}}\t{{.Repository}}" | sort -hr
```

## ⚠️ Лучшие практики

1. **Регулярный мониторинг** - используйте `docker stats` для отслеживания ресурсов
2. **Плановое обслуживание** - настройте автоматическую очистку
3. **Анализ больших образов** - идентифицируйте и оптимизируйте большие образы
4. **Ротация логов** - настройте ограничения для логов контейнеров
5. **Резервное копирование** - регулярно сохраняйте важные тома

## 🎯 Полезные команды для скриптов

```bash
# Количество работающих контейнеров
docker ps -q | wc -l

# Суммарное использование памяти
docker stats --no-stream --format "{{.MemUsage}}" | cut -d'/' -f1 | sed 's/[^0-9.]//g' | awk '{sum += $1} END {print sum " MB"}'

# Автоматическая очистка (для cron)
docker system prune -a -f --filter "until=24h"
```

Это руководство поможет эффективно управлять ресурсами Docker и поддерживать систему в оптимальном состоянии.

---
