Отлично! Это прекрасные примеры практического использования! Давай добавим их в соответствующие разделы:

---

**[file name]: 1 - Docker Compose Guide.md** (дополняем)
**[add to existing content после раздела "Настройки здоровья (Healthcheck)"]**

## 🚦 Зависимые отношения с depends_on

### Базовая настройка зависимостей

```yaml
services:
  web:
    image: nginx
    depends_on:
      - db
      - redis

  db:
    image: postgres:15

  redis:
    image: redis:alpine
```

### Расширенная настройка с условиями готовности

```yaml
services:
  web:
    image: nginx
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started

  db:
    image: postgres:15
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 3s
      retries: 5

  redis:
    image: redis:alpine
```

**Доступные условия:**

- `service_started` - сервис запущен (по умолчанию)
- `service_healthy` - сервис прошел health check
- `service_completed_successfully` - сервис завершился с кодом 0

### Практический пример для .NET приложения

```yaml
services:
  api:
    build: .
    ports:
      - "5000:80"
    depends_on:
      db:
        condition: service_healthy
      cache:
        condition: service_started
    environment:
      - ConnectionStrings__DefaultConnection=Host=db;Database=myapp

  db:
    image: postgres:15
    environment:
      POSTGRES_DB: myapp
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 3s
      retries: 10

  cache:
    image: redis:alpine
```

**Преимущества использования:**

- ✅ Избегание race conditions между сервисами
- ✅ Контроль сложных зависимостей (API → DB → Cache)
- ✅ Предотвращение ошибок "недоступная база"
- ✅ Гарантированный порядок запуска сервисов

---

**[file name]: 3 - Advanced Docker Commands.md** (дополняем)
**[add to existing content в раздел "Специализированные команды управления"]**

### `docker wait`

**Ожидание завершения контейнеров**

```bash
docker wait CONTAINER [CONTAINER...]
```

**Возвращает:** exit code контейнера (0 - успех, другие значения - ошибка)

**Примеры использования:**

```bash
# Ожидание завершения одного контейнера
docker wait migration_container

# Ожидание нескольких контейнеров
docker wait container1 container2 container3

# Использование в скриптах
EXIT_CODE=$(docker wait my_container)
if [ $EXIT_CODE -eq 0 ]; then
    echo "Контейнер завершился успешно!"
else
    echo "Контейнер завершился с ошибкой: $EXIT_CODE"
fi
```

**Практические сценарии:**

**Миграции базы данных:**

```bash
# Запуск контейнера с миграциями
docker run --name migrations myapp:migrations

# Ожидание завершения миграций
MIGRATION_EXIT=$(docker wait migrations)

if [ $MIGRATION_EXIT -eq 0 ]; then
    # Запуск основного приложения после успешных миграций
    docker run -d --name app myapp:latest
    echo "Приложение запущено после успешных миграций"
else
    echo "Миграции завершились с ошибкой. Приложение не запущено."
    exit 1
fi
```

**CI/CD пайплайны:**

```bash
# Запуск тестов в контейнере
docker run --name tests myapp:tests

# Ожидание результатов тестов
TEST_EXIT=$(docker wait tests)

if [ $TEST_EXIT -eq 0 ]; then
    echo "Тесты прошли успешно! Продолжаем деплой..."
    # Деплой в продакшен
else
    echo "Тесты упали! Останавливаем пайплайн."
    exit 1
fi
```

**Обработка данных:**

```bash
# Запуск контейнера обработки данных
docker run --name data_processor myapp:processor

# Ожидание завершения обработки
docker wait data_processor

# Запуск контейнера-анализатора результатов
docker run --name analyzer myapp:analyzer
```

**Особенности команды:**

- ⏰ Терпеливо ожидает естественного завершения контейнера
- 📊 Возвращает реальный exit code для принятия решений
- ⚡ Мгновенно возвращает код, если контейнер уже завершен
- 🔄 Работает только с запущенными контейнерами
- 🎯 Противоположность `docker kill` - ожидание вместо принудительной остановки

**Комбинация с другими командами:**

```bash
# Запуск и ожидание с логированием
docker run --name worker myapp:worker &
WORKER_PID=$!
EXIT_CODE=$(docker wait worker)
echo "Worker завершился с кодом: $EXIT_CODE"
```

---

Теперь эти практические примеры интегрированы в соответствующие разделы! 🎯

**В Compose Guide** - про зависимости между сервисами и правильную настройку порядка запуска.

**В Advanced Commands** - про ожидание завершения контейнеров в скриптах и CI/CD.

Оба примера отлично дополняют наши руководства реальными сценариями из практики! 🚀
